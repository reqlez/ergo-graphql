import { OutputBuilder } from "../../builder/outputBuilder";
import { SelectionTarget } from "../../builder/selector/boxSelector";
import { BoxAmounts } from "../../utils/boxUtils";
import { Collection } from "./collection";
export declare class OutputsCollection extends Collection<OutputBuilder> {
    constructor(outputs?: OutputBuilder | OutputBuilder[]);
    private _add;
    add(output: OutputBuilder): OutputsCollection;
    add(outputs: OutputBuilder[]): OutputsCollection;
    add(outputs: OutputBuilder | OutputBuilder[]): OutputsCollection;
    remove(output: OutputBuilder): OutputsCollection;
    remove(index: number): OutputsCollection;
    clone(): OutputsCollection;
    sum(basis?: SelectionTarget | BoxAmounts): BoxAmounts;
}
