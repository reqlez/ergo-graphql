/// <reference types="node" />
import { SigmaType } from "./sigmaType";
export declare class ByteColl extends SigmaType {
    private _bytes;
    constructor(bytes: Buffer);
    toBytes(): Buffer;
    toString(): string;
}
