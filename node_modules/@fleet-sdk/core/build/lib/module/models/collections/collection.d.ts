export declare abstract class Collection<T> implements Iterable<T> {
    protected readonly _items: T[];
    constructor();
    protected _isIndexOutOfBounds(index: number): boolean;
    [Symbol.iterator](): Iterator<T>;
    get length(): number;
    get isEmpty(): boolean;
    toArray(): T[];
}
