import { Amount, Box, BoxId } from "../../types";
import { Collection } from "./collection";
export declare class InputsCollection extends Collection<Box<bigint>> {
    constructor();
    constructor(box: Box<Amount>);
    constructor(boxes: Box<Amount>[]);
    add(box: Box<Amount>): InputsCollection;
    add(boxes: Box<Amount>[]): InputsCollection;
    add(boxes: Box<Amount> | Box<Amount>[]): InputsCollection;
    private _add;
    private _map;
    remove(boxId: BoxId): InputsCollection;
    remove(index: number): InputsCollection;
}
